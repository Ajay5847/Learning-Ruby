<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<h3>Comments</h3>
<%= render @article.comments %>

<h3>Add a Comment:</h3>
<%= render 'comments/form' %>

<%= link_to "Edit", edit_article_path, class: "btn btn-dark" %> 
<%= link_to "Destroy", article_path(@article), data: {turbo_method: :delete}, class: "btn btn-danger" %>
